# docker-compose (app + Postgres + Caddy proxy)
POSTGRES_USER=quota
POSTGRES_PASSWORD=change-me-strong-password
POSTGRES_DB=quota
AUTH_SECRET=change-me-random-long-string

# app/.env — see app/.env.example for local dev variables
